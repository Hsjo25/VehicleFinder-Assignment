services:
  backend:
    build:
      context: ./backend/VehicleFinder.Api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "5080:8080"

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
