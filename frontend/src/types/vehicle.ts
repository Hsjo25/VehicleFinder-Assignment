export interface Make {
  id: number;
  name: string;
}

export interface VehicleType {
  id: number;
  name: string;
}

export interface VehicleModel {
  id: number;
  name: string;
  makeName: string;
}
